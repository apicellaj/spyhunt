<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.apicellaj.android.spyhunt"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainscreen.MainScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".preferences.PreferencesActivity"
                  android:theme="@style/PreferencesTheme">
        </activity>
        <activity android:name=".customlocations.CustomLocationsActivity">
            <intent-filter>
                <action android:name="com.apicellaj.android.spyhunt.customlocations.CustomLocationsActivity"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".addlocation.AddLocationActivity">

        </activity>
        <activity android:name=".gamelobby.GameLobbyActivity">

        </activity>
        <activity android:name=".gamedetail.GameDetailActivity">

        </activity>
    </application>

</manifest>